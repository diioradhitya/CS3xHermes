version = 3

cloudstream {
    description = "SFlixxHermes - Movie and TV Series via TMDB + 6 iframe hosts"
    language = "id"
    authors = listOf("Dio R")
    status = 1
    tvTypes = listOf(
        "Movie",
        "TvSeries"
    )
}